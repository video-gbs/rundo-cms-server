version: '3.3'
services:
  rundo-auth-gateway:
    image: registry.cn-hangzhou.aliyuncs.com/hyy-rundo-gms/rundo-auth-gateway:latest
    container_name: rundo-auth-gateway
    restart: always
    environment:
      - TZ=Asia/Shanghai
      - env=pred
    ports:
      - 8070:8070
    volumes:
      - /tmp:/tmp
    extra_hosts:
      - "rundo-mysql:192.168.0.239"
      - "rundo-redis:192.168.0.239"
      - "rundo-nacos:192.168.0.84"
    networks:
      - proxynet
networks:
  proxynet:
    external:
      name: rundo_network
