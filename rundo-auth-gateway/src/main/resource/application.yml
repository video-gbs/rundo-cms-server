server:
  port: 8070

spring:
  application:
    name: auth-gateway
  cloud:
    nacos:
      server-addr: rundo-nacos:8848
      discovery:
        namespace: public
      config:
        file-extension: yml
  config:
    import:
      - optional:nacos:${spring.application.name}

