<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.runjian.auth.server.mapper.system.SysRoleInfoMapper">

    <select id="selectRoleByUserId" resultType="java.lang.Long">
        SELECT
        role_code
        FROM
        tb_sys_role_info roleInfo
        LEFT JOIN tb_sys_role_user userRole ON roleInfo.id = userRole.role_id
        LEFT JOIN tb_sys_user_info userInfo ON userInfo.id = userRole.user_id
        WHERE
        userInfo.id = #{userId,jdbcType=BIGINT}
    </select>
</mapper>
