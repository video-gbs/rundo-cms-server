<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.runjian.auth.server.mapper.system.SysApiInfoMapper">
    <select id="selectByRoleId" resultType="java.lang.String">
        SELECT
            url
        FROM
            tb_sys_api_info api
                LEFT JOIN tb_sys_role_api rapi ON api.id = rapi.api_id
                LEFT JOIN tb_sys_role_info role ON role.id = rapi.role_id
        WHERE
            role.id =#{roleId,jdbcType=BIGINT}
    </select>
</mapper>
