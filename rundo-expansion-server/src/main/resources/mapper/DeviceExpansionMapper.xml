<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.runjian.device.expansion.mapper.DeviceExpansionMapper">


    <select id="listPage" resultType="com.runjian.device.expansion.entity.DeviceExpansion">
        SELECT
            *
        FROM
            `rundo_device_expansion`
        WHERE
            deleted = 0

    </select>
</mapper>
